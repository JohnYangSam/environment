# Disguise as IE 9 running on on Windows 7
user-agent = "Mozilla/5.0 (compatible; MSIE 9.0; Windows NT 6.1; Trident/5.0)"

# Automatically set the previous URL as referer when following a redirect
referer = ";auto"
